# Getting Started (Draft)

1. Create a virtual environment.
2. Install dependencies with `pip install -r requirements-dev.txt`.
3. Run `pytest` to ensure the scaffold passes tests.
4. Explore `examples/` for usage.
