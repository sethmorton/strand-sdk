# Manifests

Manifests capture every run with fields: `run_id`, `timestamp`, `experiment`, `inputs`, `optimizer`, `reward_blocks`, and `results`. See `strand/manifests/manifest.py` for the dataclass.
